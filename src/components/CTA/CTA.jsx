import styled from "styled-components";

import theme from "../../styles/theme";

const CTA = styled.h1`
  font-size: 40px;
  font-weight: 900;
  color: ${theme.colors.ink.light};
  width: 100%;
  line-height: 120%;
`;

export default CTA;
